// remove that variables - it leads to error now!
var renderWidth = 800;
var renderHeight = 600;

var tilemap = null;
var menu = null;
var menuBarWidth = 120;

$(document).ready( function () {
    new Evobox();
});